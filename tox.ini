[tox]
envlist = py27, py35, flake8

[testenv]
commands = pytest --cov=is_wire.core {posargs:tests}
deps = 
  -r{toxinidir}/requirements-test.txt

[testenv:flake8]
commands = flake8 src/is_wire/core
deps = 
  flake8
